---
title: 'PG가 5초 늦거나 답이 없을 때, 결제 승인은 어떻게 되나'
date: '2026-09-05'
tags: ['프로젝트', '결제', '부하테스트', '서킷브레이커', 'HikariCP', 'k6']
draft: true
summary: '가짜 PG로 지연과 무응답을 주입해 결제 승인 API의 한계를 재현하고, 커넥션 풀 고갈의 진짜 원인을 찾아 고친 기록입니다. 개선 전과 후를 같은 조건으로 측정했습니다.'
---

### Introduction

<TOCInline toc={props.toc} exclude="Introduction" />

## 왜 이 테스트를 했나

결제 사고 사례를 찾아보면 대부분이 같은 유형이다. 우리 서버가 아니라 PG나 카드사가 느려지거나 멈춘다. 그때 우리 서버가 같이 멈추면 결제 한 건의 문제가 서비스 전체의 문제가 된다.

우리 서비스는 트래픽 때문에 난 장애가 없었다. 그래서 PG가 5초씩 늦으면, 아예 답을 안 하면 승인 API가 어떻게 되는지 숫자로 몰랐다. 코드를 읽으면 "괜찮을 것 같다"까지는 말할 수 있지만 "동시 50건에서 몇 건이 살아남는다"는 말할 수 없었다.

그래서 진짜 PG 대신 가짜 PG를 붙여 지연과 무응답을 주입하고, 개선 전과 후를 같은 조건으로 재기로 했다. 원칙은 하나였다. **개선하기 전에 먼저 잰다.** 전 수치가 없으면 후 수치는 성과가 아니다.

## 테스트 A. PG 응답 5초 지연, 동시 승인 50건 → 100건

**한 줄로 :** PG가 5초씩 늦게 답하는 상황에서 50명이 동시에 결제하면, 승인 요청 대부분이 30초를 기다리다 실패했다. 원인은 PG가 아니라 우리 서버의 DB 커넥션이었다.

조건은 dev 서버 한 대, DB 커넥션 풀 10개(동시에 DB를 쓸 수 있는 요청이 10개라는 뜻), 톰캣 스레드 200. 부하는 k6로 테스트 회원 30명이 츄르 충전 주문을 만들고 동시에 승인을 요청했다. 가짜 PG 응답을 5초 늦추고 동시 50건, 90초 뒤 동시 100건을 넣었다. 총 150건.

아래 표의 p95는 "100건 중 95번째로 느린 요청이 걸린 시간"이다. 평균보다 느린 쪽 꼬리를 본다.

### 전 : 트랜잭션 안에서 PG를 부르던 옛 구조

지금 코드는 토스 호출을 트랜잭션 밖으로 뺀 상태다. 예전 구조(트랜잭션 하나로 승인 전체를 감싸는)를 재현하려고 프로퍼티 스위치를 하나 두고 켰다.

| 항목                      | 결과                                |
| ------------------------- | ----------------------------------- |
| 승인 성공                 | **20 / 150 (13%)**                  |
| 승인 소요                 | min 30.05초, 중앙값 36초, p95 100초 |
| 가짜 PG에 도달한 요청     | 20                                  |
| 커넥션 대기 타임아웃 로그 | 172건                               |
| 풀 대기(pending)          | 50 → 100                            |

150건 중 20건만 PG에 닿았다. 나머지 130건은 PG를 부르기도 전에 죽었다. 타임아웃 로그에 찍힌 우리 코드의 첫 줄이 전부 "트랜잭션 시작"이다. 옛 구조는 트랜잭션을 열면서 DB 커넥션을 먼저 잡고, 그 커넥션을 쥔 채 PG 응답 5초를 기다린다. 커넥션이 10개니 50명 중 10명만 받고, 40명은 줄을 서서 30초를 기다리다 죽는다.

![image.png](/static/images/PG_지연_1.png)

- 커넥션 풀 패널에서 대기(pending)가 50에서 100으로 선다. 최대는 10에 고정이다.
- 커넥션 대기 타임아웃이 분당 120건 가까이 쌓인다. 승인 결과는 success 0.4 req/s가 전부다.

<br />

![image.png](/static/images/PG_지연_2.png)

- HTTP 500이 3 req/s로 나가는 동안 CPU는 20%를 넘지 않는다. 서버가 바쁜 게 아니라 커넥션을 기다리고 있었다.
- 스레드가 32에서 140으로 늘어난 건 요청 스레드가 대기열에 묶여 있어서다.

### 후 : 트랜잭션을 분리한 현재 코드

스위치를 끄고 같은 부하를 넣었다. 예상은 "다 살아남는다"였다.

| 항목                      | 결과                                    |
| ------------------------- | --------------------------------------- |
| 승인 성공                 | **27 / 150 (18%)**                      |
| 승인 소요                 | min 11.2초, 중앙값 71초, p95 95초       |
| 가짜 PG에 도달한 요청     | 150                                     |
| 승인 뒤 보상 취소         | 123                                     |
| 커넥션 대기 타임아웃 로그 | 606건                                   |
| 주문 끝 상태              | PAID 27, FAILED 83, 결제 진행중 잔류 40 |
| 알림                      | 성공 알림 8/27, 영수증 23/27            |

트랜잭션 분리는 제 역할을 했다. 150건 전부 PG에 닿았다. 그런데 승인은 82%가 실패했고 123건은 승인 뒤 보상 취소로 돌아갔다. 고객 관점에선 카드 승인 문자와 취소 문자가 연달아 온다.

![image.png](/static/images/PG_지연_3.png)

- 대기(pending)가 50, 50, 100으로 세 번 선다. 50건 단계에서 두 번, 100건 단계에서 한 번이다.
- 승인 결과 패널의 라벨이 `db_failed`다. PG는 승인했고 우리 DB 저장에서 죽었다는 뜻이다. 실패 지점이 PG에서 DB로 옮겨 왔다.

<br />

![image.png](/static/images/PG_지연_4.png)

- 스레드 140, CPU 40%. 전보다 CPU를 더 쓴 건 150건 전부 PG까지 갔다가 돌아왔기 때문이다.

### 원인 : 커밋 직후 알림 처리가 두 번째 커넥션을 요구한다

용어 둘만 짚고 간다. 결제가 저장되면 "결제 완료 알림"과 "영수증 알림"을 보내는 코드가 있다. 이 코드는 결제 커밋이 끝난 직후에 자동으로 실행되고(AFTER_COMMIT), 알림을 저장할 때 자기만의 새 트랜잭션을 연다(REQUIRES_NEW). 새 트랜잭션은 DB 커넥션을 하나 더 필요로 한다.

타임아웃 로그 606줄을 "어디서 기다리다 죽었나"로 분류했다.

| 대기하다 죽은 지점          | 건수 |
| --------------------------- | ---- |
| 결제 저장 트랜잭션 진입     | 160  |
| 실패 처리(주문 FAILED 전환) | 40   |
| 알림 리스너                 | 33   |
| JWT 회원 조회               | 2    |

순서대로 보면 이렇다.

1. PG가 5초 뒤 50건에 동시에 답한다. 50건이 동시에 "결제 저장"에 들어온다.
2. 커넥션 10개를 받은 10건이 저장을 끝내고 커밋한다.
3. **커밋 직후 알림 처리가 시작되고, 알림을 저장하려고 커넥션을 하나 더 요청한다.** 그런데 첫 커넥션은 아직 반납 전이다.
4. 10건 모두 첫 커넥션을 쥔 채 두 번째 커넥션을 기다린다. 풀에 남은 커넥션은 0개. 서로가 서로를 기다린다.
5. 나머지 40건은 저장에 들어가지도 못하고 30초를 기다리다 죽는다.
6. 죽은 40건은 PG에 이미 승인된 돈을 되돌리는 보상 취소를 부른다. 취소 뒤 주문을 "실패"로 바꾸는 것마저 커넥션을 못 받아, 주문이 "결제 진행중"으로 남는다.

![image.png](/static/images/다이어그램_1.png)

처음엔 이 가설을 버렸다. 로그를 에러마다 40줄씩 잘라 "알림 처리" 줄을 찾았는데 0건이었다. 실제 스택은 40줄보다 길어서 잘린 부분에 있었다. 60줄로 다시 자르고, 알림 실패 로그 40건의 원인이 전부 "트랜잭션을 열 커넥션을 못 받음"(`Could not open JPA EntityManager for transaction`)인 것을 확인한 뒤에 가설이 섰다. 측정 도구가 아니라 내가 로그를 자른 폭이 틀렸던 것이다.

> [!NOTE]
> 캡처 3 자리 (없음, 선택). 앱 로그의 타임아웃 스택 첫 프레임 분류 grep 출력. 위 표가 그 결과라 캡처 없이 표로만 가도 된다. 넣으려면 `PG_지연_13.png`.

### 대안 검토

- **커넥션 풀 확대 (10 → 20)** : 동시 커밋이 풀의 절반을 넘으면 같은 방식으로 다시 막힌다. 구조를 안 바꾸면 무너지는 숫자만 옮긴다.
- **알림 저장을 결제 트랜잭션 안에서 같이** : 커넥션은 안 늘지만, 알림 저장이 실패하면 결제 저장까지 함께 되돌아갈 위험이 있다. 알림 중복을 잡는 처리가 트랜잭션 전체를 취소 대상으로 표시하기 때문이다. 실제 로그에 그런 이력(`UnexpectedRollbackException`)이 있었다.
- **채택 : 알림 처리를 다른 스레드로 넘긴다** : 결제를 처리한 스레드는 커밋 직후 커넥션을 반납하고 끝낸다. 알림은 알림 전용 스레드(최대 4개)가 나중에 자기 커넥션으로 처리한다. 알림이 실패해도 결제는 그대로고, 알림이 커넥션을 4개 넘게 쓰는 일도 없다.

### 후2 : 알림 리스너 비동기 적용

| 항목                 | 전 (옛 구조) | 후 (트랜잭션 분리) | 후2 (+ 리스너 비동기)         |
| -------------------- | ------------ | ------------------ | ----------------------------- |
| 승인 성공 / 150      | 20           | 27                 | **150**                       |
| 승인 소요 p95        | 100초        | 95초               | **6.07초** (PG 지연 5초 포함) |
| 커넥션 대기 타임아웃 | 172          | 606                | **0**                         |
| 보상 취소            | 0            | 123                | **0**                         |
| 알림 성공 / 영수증   | 0/20, 10/20  | 8/27, 23/27        | **150/150, 150/150**          |
| 원장 항등식          | 20 = 20+0+0  | 150 = 27+123+0     | 150 = 150+0+0                 |

원장 항등식은 가짜 PG 원장의 승인 건수 = 우리 DB의 PAID 주문 + 원장의 취소 + 사고 기록. 이 식이 안 맞으면 돈이 어디로 샌 것이다. 세 번 모두 맞았다.

동시 100건이 6.13초 안에 전부 끝났다. PG 지연 5초를 빼면 우리 서버 몫은 1초 남짓이다. 처리량으로 쓰면 100건 ÷ 6.13초 = 16 TPS인데, 이 숫자는 PG 지연에 묶인 값이라 서버 용량이 아니다. 용량은 PG 정상 응답 조건에서 따로 잴 예정이다.

> [!NOTE]
> 캡처 4 자리 (아직 없음). Grafana 시간 범위를 2026-09-04 16:17 ~ 16:22 로 두고 상단 4패널 → `PG_지연_14.png`. pending 이 0 으로 평평하고 success 만 선다. Prometheus 가 어제 데이터를 갖고 있어 다시 돌리지 않고 찍을 수 있다.

### 덤으로 찾은 것 : 잔류 주문을 영원히 보류하던 복구 배치

후 테스트에서 결제 진행중으로 남은 40건은 10분마다 도는 복구 배치가 정리해야 한다. 그런데 배치는 PG 상태가 CANCELED면 "판단 보류"로만 처리하고 있었다. 보상 취소된 주문은 PG 상태가 항상 CANCELED라 영원히 풀리지 않는다. 배치에 CANCELED → FAILED 분기를 추가했다.

## 테스트 B. PG 무응답, 서킷브레이커 전과 후

**한 줄로 :** PG가 아예 답을 안 하면 결제 요청 하나가 30초씩 서버를 잡고, 건마다 사고 기록과 알람이 쌓였다. 서킷브레이커를 달아 "PG가 죽었다"고 판단되면 PG를 부르지 않고 바로 실패를 돌려주게 했다.

서킷브레이커는 전기 차단기와 같은 뜻이다. 실패가 일정 비율을 넘으면 회로를 끊어(열림) 더 이상 PG를 부르지 않고, 잠시 뒤 몇 건만 흘려보내(반열림) 살아났으면 다시 붙인다(닫힘).

가짜 PG를 무응답 모드로 두고 동시 50건을 넣었다.

### 전 : 요청 하나가 30초를 잡는다

| 항목                  | 결과                              |
| --------------------- | --------------------------------- |
| 승인                  | 50 / 50 실패, 전부 30.2 ~ 30.5초  |
| 가짜 PG에 매달린 요청 | 100 (승인 50 + 결과 확정 조회 50) |
| 사고 기록             | +50                               |
| CRITICAL 알람 로그    | 100줄                             |

PG가 답을 안 하면 승인 요청은 15초를 기다리고 포기한다. 그런데 승인이 실제로 됐는지 모르는 상태라 "이 주문 승인됐나"를 PG에 한 번 더 물어보고, 그것도 15초를 기다린다. 그래서 요청 하나가 30초 동안 서버 스레드를 잡는다. 두 번째 조회까지 실패하면 설계상 사고 기록을 남기고 운영자에게 알람을 보내게 돼 있어서, 건마다 사고와 메일이 쌓인다. 서버가 동시에 처리할 수 있는 요청이 200개니, 동시 200건이면 서버 전체가 30초씩 멈춘다.

> [!NOTE]
> 캡처 B-전 자리 (아직 없음). Grafana 16:30 ~ 16:34 상단 4패널 → `PG_지연_15.png`. 커넥션 타임아웃은 0 인데 결과가 전부 `failed`, 응답 시간 P50 이 30초에 찍힌다. 커넥션이 아니라 PG 대기에서 30초가 갔다는 증거.

### 대안 검토

- **기다리는 시간 단축** : 정상 승인도 가끔 느리다. 짧게 자르면 성공할 결제까지 실패로 만든다.
- **재시도** : 죽은 PG에 부하만 더한다.
- **채택 : 서킷브레이커** : 실패가 쌓이면 PG를 부르지 않고 바로 실패를 돌려준다. 승인 호출에만 건다. 취소와 조회는 돈을 되돌리는 복구 경로라 차단하지 않는다.

설정은 이렇다. 최근 20건을 보고, 그중 절반 넘게 실패하면 열린다(판단하려면 최소 10건은 있어야 한다). 열린 뒤 30초가 지나면 3건만 시험으로 흘려보내고, 다 성공하면 닫힌다. 카드 한도 초과 같은 거절(4xx)은 PG가 정상이라는 뜻이라 실패로 세지 않는다. 열려 있는 동안은 승인 자체를 안 하니 돈이 움직이지 않고, 조회도 취소도 사고 기록도 없다.

![image.png](/static/images/다이어그램_2.png)

### 후 : 동시 20건 → 40초 뒤 50건

| 항목                  | 결과                                                                |
| --------------------- | ------------------------------------------------------------------- |
| 1단계 20건            | 30.5초 실패 (실패 10건이 쌓이기 전이라 차단기가 아직 닫힘 상태)     |
| 2단계 50건            | **전부 PG 호출 없이 즉시 실패, 가장 빠른 건 0.22초, 중앙값 0.49초** |
| 차단기 상태 변화      | 16:50:48 닫힘 → 열림, 16:51:18 열림 → 반열림                        |
| 가짜 PG에 매달린 요청 | 40 (1단계만)                                                        |
| 사고 기록             | +20 (2단계는 0)                                                     |

첫 20건은 그대로 30초를 겪는다. 차단기는 뒤따르는 요청을 보호하는 것이라 이건 한계이고 결함은 아니다. "PG가 20분 죽었다면"으로 바꿔 말하면, 전에는 20분 내내 요청마다 30초와 사고와 알람이고, 후에는 첫 30초만 겪고 이후 즉시 실패하며 30초마다 3건씩만 살아났는지 시험한다.

![image.png](/static/images/PG_지연_5.png)

- 커넥션 대기 타임아웃 0. 무응답 테스트는 커넥션이 아니라 PG 대기가 병목이라 풀 그래프는 조용하다.
- 16:51에 대기(pending)가 29로 한 번 튄다. 차단기가 열린 순간 50건이 동시에 즉시 실패 처리되며 주문 상태를 바꾸는 DB 쓰기가 몰린 것이다.

<br />

![image.png](/static/images/PG_지연_6.png)

- HTTP 503이 새로 보인다. 차단기가 열려 PG 호출 없이 즉시 실패시킨 응답이다. 16:48~16:49의 빈 구간은 서킷브레이커 배포로 프로세스를 재기동한 시간이다.

> [!NOTE]
> 캡처 5 보강 (아직 없음). 위 캡처는 결과 패널 눈금이 100 req/s 라 선이 안 보인다. Grafana 16:49 ~ 16:53 으로 좁혀 다시 찍으면 `pg_unavailable` 이 서는 게 보인다 → `PG_지연_16.png`. Prometheus Graph 에 `resilience4j_circuitbreaker_state{state="open"}` 을 2026-09-04 07:49 ~ 07:53 UTC 로 → `PG_지연_17.png`.

## 테스트 C. 실패율 경보는 몇 분 만에 아나

**한 줄로 :** PG 장애가 나면 사람이 알아채기까지 걸리는 시간을 재봤다. 경보 규칙을 하나 두었더니 2분 3초 만에 울렸다.

전에는 경보가 없었다. PG 장애를 누군가 결제가 안 된다고 말해 줄 때까지 모른다. 규칙을 하나 두었다. 최근 5분 동안 승인 실패율이 20%를 넘고 실패가 5건 이상이면, 그 상태가 1분 이어질 때 경보를 보낸다.

테스트 B의 후 실행에서 실측했다. 무응답 주입 07:50:12, 첫 실패 07:50:48, 경보 pending 07:51:15, **firing 07:52:15**, 해소 07:53. 장애 주입 기준 2분 3초, 첫 실패 기준 1분 27초 만에 감지했다.

배운 것 하나. 차단기가 바로 거절한 50건은 "PG 사용 불가"라는 새 종류의 실패로 집계되는데, 그 종류가 그 순간 처음 생겨서 Prometheus가 "0에서 50으로 늘었다"를 계산하지 못했다. 처음 나타난 값의 증가분은 모르기 때문이다. 경보는 그 앞의 일반 실패 20건만으로 울렸다. 서버가 켜질 때 실패 종류마다 0을 미리 등록해 두면 해결된다. 후속으로 남겼다.

> [!NOTE]
> 캡처 6 자리 (아직 없음). Prometheus Graph 에 `ALERTS{alertname="PaymentConfirmFailureRateHigh"}` 를 2026-09-04 07:49 ~ 07:55 UTC 로 → pending·firing 이 계단으로 보인다 → `PG_지연_18.png`.

## 왜 동시 50건과 100건인가

- 병목 후보였던 커넥션 풀 10개의 5배와 10배. 풀보다 조금 큰 부하면 "가끔 느려짐"으로 끝나 원인이 안 갈리는데, 5배면 풀 고갈이면 확실히 무너진다. 실제로 82%가 실패했다.
- 실서비스 순간 피크보다 몇 배 센 조건이다. 승인 한 경로에 동시 50 ~ 100건은 실서비스 전체 API 합보다 크다.
- 테스트 환경 한도. 승인 API는 회원당 분당 10건 제한이라 테스트 계정 30개로 한 번에 넣을 수 있는 승인이 최대 300건이고, 그 안에서 50 → 100 두 단계로 잡았다.

## 남은 것

- 용량 측정 : PG 정상 응답(300ms) 조건에서 50 → 100 → 200 → 300으로 올려 p95가 꺾이는 지점과 TPS를 잰다. 지금의 16 TPS는 PG 지연 5초에 묶인 값이다.
- prod 경보 : 승인 결과 지표를 CloudWatch로 보내고 같은 규칙을 등록한다.
- 카운터 사전 등록 : 라벨별 카운터를 기동 시 0으로 만들어 첫 증가가 사라지지 않게 한다.
- 다음 테스트 : 새벽 구독 갱신 배치가 순차 처리라 건수에 정비례하고, 자정을 넘기면 다음 날 배치와 겹쳐 이중 결제가 나는지.

## 정리

| 문제                                             | 개선                      | 전 → 후                                                           |
| ------------------------------------------------ | ------------------------- | ----------------------------------------------------------------- |
| PG 5초 지연에 동시 50건이면 승인이 30초씩 멈춤   | 알림 리스너 비동기 분리   | 승인 성공 27/150 → 150/150, p95 95초 → 6.07초, 알림 유실 23건 → 0 |
| PG 무응답이면 요청마다 30초 점유, 사고 기록 폭주 | 승인 호출에 서킷브레이커  | 실패 응답 중앙값 30.4초 → 0.49초, 사고 기록 50 → 0                |
| PG 장애를 사람이 알 때까지 모름                  | 실패율 경보               | 장애 주입 2분 3초 만에 감지                                       |
| 보상 취소된 주문이 결제 진행중으로 영원히 남음   | 복구 배치에 CANCELED 처리 | 잔류 40건 → 배치가 회수                                           |

트랜잭션 분리는 PG 도달까지만 보장했다. 그 뒤에 숨어 있던 두 번째 커넥션 요구는 부하를 실제로 걸어 보기 전엔 보이지 않았다. 코드를 읽어서 아는 것과 재서 아는 것은 다르다.
